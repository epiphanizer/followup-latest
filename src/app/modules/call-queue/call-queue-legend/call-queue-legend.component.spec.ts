import { CallQueueLegendComponent } from './call-queue-legend.component';

describe('CallQueueLegendComponent (Jest)', () => {
  it('constructs without Angular TestBed', () => {
    const comp = new CallQueueLegendComponent();
    expect(comp).toBeTruthy();
  });
});
